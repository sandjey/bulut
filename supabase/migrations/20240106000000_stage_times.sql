-- ============================================================
--  Миграция: время по этапам (сколько карточка стояла в каждой колонке)
--  Выполните в Supabase SQL Editor. Идемпотентно.
-- ============================================================

alter table public.tasks add column if not exists stage_times jsonb not null default '{}'::jsonb;
