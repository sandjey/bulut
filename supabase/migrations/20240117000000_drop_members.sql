-- ============================================================
--  Убираем таблицу members — единственный источник людей теперь profiles.
--  Опционально. Приложение уже не использует members (команда, выбор
--  исполнителя и «Я —» берутся из profiles). Выполните в Supabase SQL Editor,
--  если хотите оставить в базе один источник правды.
--
--  ВНИМАНИЕ: удаляет таблицу и её данные безвозвратно.
-- ============================================================

drop table if exists public.members cascade;
